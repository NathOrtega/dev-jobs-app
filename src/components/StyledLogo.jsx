import styled from "styled-components";

export const StyledLogo = styled.a`
  text-decoration: none;
  color: var(--Light100);
  font-weight: bold;
  font-size: 30px;
  cursor: pointer;

  &:visited {
    color: var(--Light100);
  }
`